"use client"
export default function Search({ accessToken }: { accessToken: string }) {

    return (
        <div>
            Hello World?
            {accessToken}
            <input />
        </div>
    )
}